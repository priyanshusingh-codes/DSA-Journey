#include<iostream>
#include<vector>//means you are importing the C++ Standard Library vector container.
using namespace std;
int main()
{
    vector<int>vec = {1, 2, 3};// size = 0 by default if not arguments declared
    cout << vec[0];
    return 0;
}
